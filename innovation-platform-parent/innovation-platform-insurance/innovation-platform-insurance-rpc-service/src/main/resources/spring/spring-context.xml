<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
	xmlns:aop="http://www.springframework.org/schema/aop"
	xsi:schemaLocation="
		http://www.springframework.org/schema/beans
		http://www.springframework.org/schema/beans/spring-beans-3.2.xsd
		http://www.springframework.org/schema/context
		http://www.springframework.org/schema/context/spring-context-3.2.xsd
		http://www.springframework.org/schema/aop
		http://www.springframework.org/schema/aop/spring-aop-3.2.xsd"
	default-autowire="byName" default-lazy-init="false">

	<!-- 采用注释的方式配置bean -->
	<context:annotation-config />

	<!-- 配置要扫描的包 -->
	<context:component-scan base-package="cn.innovation.platform.insurance.service" />

	<!-- proxy-target-class默认"false",更改为"ture"使用CGLib动态代理 -->
	<aop:aspectj-autoproxy proxy-target-class="true" />

	<bean id="insuranceApiHelper" class="cn.innovation.platform.insurance.common.utils.InsuranceApiHelper">
		<constructor-arg name="mobileUrl" value="${city.mobileUrl}" />
		<constructor-arg name="damdataUrl" value="${insurance.damdataUrl}" />
		<constructor-arg name="damdataSecret" value="${insurance.damdataSecret}" />
		<constructor-arg name="vinsunUrl" value="${insurance.vinsunUrl}" />
		<constructor-arg name="vinsunMchId" value="${insurance.vinsunMchId}" />
		<constructor-arg name="vinsunMedia" value="${insurance.vinsunMedia}" />
		<constructor-arg name="vinsunSecret" value="${insurance.vinsunSecret}" />
	</bean>

	<import resource="classpath:spring/spring-dubbo-provider.xml" />
	<import resource="classpath:spring/spring-mybatis.xml" />
	<import resource="classpath:spring/spring-redis.xml" />
</beans>